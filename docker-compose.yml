services:
  tracea:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    environment:
      - TRACEA_DB_PATH=/data/tracea.db
      - TRACEA_RULES_PATH=/data/detection_rules.yaml
      - TRACEA_ALERTS_PATH=/data/alerts.yaml
      - TRACEA_DATA_DIR=/data
      - TRACEA_RETENTION_DAYS=30
    restart: unless-stopped
